#if INTERACTIVE
#else
namespace FSharpExercises
#endif

module UndergroundExercises = 

   // You can 'open' a module like a namspace:
   open Underground

   // Which stations contain "East"?
   let EastStations() =
      // Create a function which returns an array of stations containing "East"
      ()

   // Which stations contain "North", "South", "East" or "West"?
   let CompassStations() =
      // Create a function which returns an array of stations containing "North", "South", "East" or "West".
      // (Don't worry about constraining to whole words.)
      ()